name: Build and Deploy to Minikube
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build-and-deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: test
        run: |
          hostname
          whoami
        

     

      
